# Simple workflow for deploying static content to GitHub Pages
name: Deploy static content to Pages

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["main"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Single deploy job since we're just deploying
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Pages
        uses: actions/configure-pages@v5

      # Only publish the actual website. Source files (scripts/, .github/,
      # *.py, .DS_Store, .gitignore, ...) must never reach the public site.
      - name: Build public site
        run: |
          set -euo pipefail
          rm -rf _site
          mkdir -p _site/event _site/training _site/contact _site/dino _site/assets
          cp index.html _site/index.html
          cp event/index.html _site/event/index.html
          cp training/index.html _site/training/index.html
          cp event.html _site/event.html
          cp schedule.json _site/schedule.json
          cp event.json _site/event.json
          cp contact/qr-code.png _site/contact/qr-code.png
          cp dino/dino.png dino/favicon.gif dino/favicon.png _site/dino/
          cp assets/modals.css assets/modals.js _site/assets/
          touch _site/.nojekyll
          echo "Published files:"
          find _site -type f | sort

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Upload only the curated public site
          path: '_site'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
